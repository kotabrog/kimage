# Agent Guidelines

このドキュメントは、Rust プロジェクトでの実装・テスト・実行ルールをまとめたものです。

## 基本方針

- 実装は小さな単位で段階的に進める。
- 1ブランチの責務を小さく保つ。
- 異なる責務の実装を不用意に混ぜない。
- 公開 API は、利用側のコードが自然に書ける最小形から始める。
- 小さく独立したユニットテストを基本とし、挙動の意図を明確にする。
- 実装の公開 API に沿った順序でテストを並べ、読みやすさと保守性を高める。

## テストの配置と順序

- `#[cfg(test)] mod tests { ... }` を基本に、同一ソースファイル内にユニットテストを配置する。
- テストの並び順は「実装している関数・メソッドの順番」に合わせる。
- ファイル内で閉じている“統合テスト的”なテスト（複数の API をまたいで検証するもの）は最後に配置する。
  - テスト名に `integration_` プレフィックスを付け、意図を明確にする。

## テストの粒度

- 原則、1テストで1つの関数・メソッドの挙動を検証する。
  - 複数関数をまたぐ検証は「統合テスト枠」に分離する。
- 同一関数の正常系・境界値・異常系は、テストケースを分けて記述する。
- エラー系は、返るエラーの種類と主要フィールドを確認する。
- `std::io::Error` を返す helper では、必要に応じて `ErrorKind` を確認する。

## モジュール構成

- ファイルやモジュールは責務ごとに分ける。
- `src/lib.rs` は module 宣言と公開 re-export を中心に保つ。
- 共通型、エラー型、IO helper、機能別実装などは、それぞれ独立したモジュールに分ける。
- あるファイル内に閉じるロジックのユニットテストは、同じファイルの `#[cfg(test)] mod tests` に置く。

## ブランチ運用

- 作業ブランチは小さな目的ごとに切る。
- ブランチ名は短い prefix を使う。
  - 例: `feat/...`, `fix/...`, `docs/...`, `chore/...`
- 作業ブランチの分岐元やマージ先は、プロジェクトの運用に合わせる。

## CI/実行

- Makefile ターゲットを利用する。
  - フォーマット検証: `make fmt-check`
  - Lint: `make lint`（Clippy 警告・rustc 警告をエラー化）
  - テスト: `make test`
  - CI まとめ: `make ci`（format, lint, test をまとめて実行）
- GitHub Actions の対象ブランチやジョブ構成は、プロジェクトの運用に合わせる。
